create(v) { //v is a vendor object
  // no return
}
  
  read(id) { //id is the key of a vendor
  // return vendor object
}
  
  update(v) { //v is a vendor object
  // replace existing row(s)
  // no return
}
  
  // renamed to remove because delete is a JS keyword
  remove(id) { //id is the key of a vendor
  // no return
}
  
  list() { //no params
  // return array of vendor objects
}