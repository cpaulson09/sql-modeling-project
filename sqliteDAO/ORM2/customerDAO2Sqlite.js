// ORM 1 sqlite customers

create = (customer) => {
    // no return
}

read = (id) => {
    // return vendor object
}

update = (customer) => {
    // replace existing row(s)
    // no return
}

remove = (id) => {
    // no return
}

list = () => {
    // return array of objects
}


module.exports = {create, read, update, remove, list}
